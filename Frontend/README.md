
  # High-Fidelity Mobile UI Prototype

  This is a code bundle for High-Fidelity Mobile UI Prototype. The original project is available at https://www.figma.com/design/DPBXsjM3Sstj3gb6ukEYh4/High-Fidelity-Mobile-UI-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  